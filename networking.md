# Networking
---
## IPv4 & IPv6

- **IPv4**
    - 현재 가장 널리 사용되는 표준 주소 체계
    - 32비트로 구성되며 8비트씩 4부분으로 나누어 .으로 구분함
    - 2의 32승, 약 43억개의 주소를 만들 수 있음
- **IPv6**
    - IPv4의 주소 부족 문제를 해결하기 위해 등장한 차세대 주소 체계
    - 128비트로 구성되며 16비트씩 8부분으로 나누어 :으로 구분함
    - 2의 128승, 약 340간 이라는 엄청나게 많은 수의 주소를 만들 수 있음

#### 왜 아직 IPv4를 사용할까?
전 세계의 수많은 장비가 아직 많이 IPv4를 사용하기 떄문에
두 체계를 동시에 사용하는 **듀얼스택** 기술 등을 활용하며 서서히 IPv6로 넘어가는 추세

**듀얼스택 기술**: 장비의 네트워크 인터페이스에 IPv4 와 IPv6의 주소를 모두 설정해 상대 서버에 따라 IPv4/IPv6 로 통신하는 방식 둘 다 지원시 IPv6 우선 지원

---
## ARP/RARP(Address Resolution Protocol/Reverse ARP)
**ARP**
> IP주소를 MAC주소로 변환해주는 프로토콜

**ARP의 동작 방식**
1. ARP Request
    - 네트워크 전체에 브로드캐스트로 IP주소에 대한 MAC 주소를 가진 기기를 찾음

2. ARP Reply
    - 해당 IP를 가진 기기가 자신의 MAC 주소를 보냄

**RARP**
> 자신의 MAC 주소는 아는데 IP주소는 모를 떄 서버로부터 IP주소를 요청하기위해

**왜 RARP를 사용했을까?**
예전에는 보조기억장치가 없는 디스크리스 워크스테이션을 사용했는데 부팅 직후 자신의 IP주소를 알 수 없어 서버에 자신의 MAC 주소를 보내고 IP주소를 받음

**사용하지 않는 이유**
RARP는 전용 서버도 필요하고, IP주소 외의 정보를 알려주지 못했으며 모든 네트워크의 설정값을 알려주는 DHCP가 나오며 더이상은 사용하지 않음

***DHCP(Dynamic Host Configuration Protocol)\***
> 네트워크에 접속하려는 장치에게 복잡한 네트워크 설정값을 자동으로 할당해주는 프로토콜
 
 **작동 방식 4단계**
 1. 탐색(Discover)
    - 클라이언트가 서버에게 IP 요청
2. 제안(Offer)
    - DHCP 서버가 남는 주소를 제안함
3. 요청(Request)
    - 클라이언트가 주소를 쓸 것을 결정
4. 확인(Acknowledge)
    - DHCP서버가 사용 승인

(참고: 임대시간동안 IP를 빌려주는 개념)

---

## 3way handshake
> TCP 프로토콜에서 서버와 클라이언트와의 연결을 확인하고 데이터를 보내기 위한 절차

양 측 모두 데이터를 보낼 준비가 되었음을 확인하고 시퀀스 넘버를 동기화하여 **연결을 수립하고 신뢰성을 확보함**

**작동 순서 3단계**
1. **SYN**
    - 난수 기반의 시퀀스 넘버를 패킷에 담아 서버에 보냄(신청)
2. **SYN + ACK**
    - 수신한 클라이언트의 시퀀스 넘버+1을 ACK값으로 설정해 서버도 시퀀스 넘버를 생성하여 SYN와 ACK를 모두 담은 패킷을 전송 (요청 확인 후 재전송)
3. **ACK**
    - 클라이언트는 서버의 SYN + ACK를 받으면, 서버의 Sequence number + 1을 ACK 값으로 설정하여 서버에 전송(최종 확인)


## 4way handshake
> 데이터 전송이 완료된 후, 안전하게 연결을 종료하는 과정

**어느 한쪽에 남아있는 데이터가 있을 수 있으니 안전하게 통신을 종료하는 단계**

1. **FIN**
    - 클라이언트에서 연결 종료 신호를 보냄
2. **ACK**
    - 서버가 확인 후 남아있는 데이터 처리
3. **FIN**
    - 서버가 클라이언트에게 종료 신호를 보냄
4. **ACK**
    - 클라이언트가 종료신호 확인 후 일정시간 대기하고 연결 종료(TIME_WAIT)

반드시 클라이언트가 먼저 요청하는 건 아님

**플래그 명칭**
- **SYN(Synchronize)**
    - 연결 시작 시 시퀀스 번호 동기화
- **ACK(Acknowledgment)**
    - 패킷을 받았다는 응답을 보낼 때 사용
- **FIN(Finish)**
    - 데이터 전송을 마치고 연결 종료
- **RST(Reset)**
    - 강제종료
- **PSH(Push)**
    - 버퍼가 찰 때까지 기다리지 않고 즉시 전달
- **URG(Urgent)**   
    - 다른 데이터보다 우선 처리 필요 시 사용
 
## 3way handshake vs 4way handshake
| 구분 | 3-way Handshake | 4-way Handshake |
| :--- | :--- | :--- |
| **주요 목적** | **연결 성립** (Connection Establishment) | **연결 해제** (Connection Termination) |
| **단계 수** | **3단계** (SYN → SYN+ACK → ACK) | **4단계** (FIN → ACK → FIN → ACK) |
| **패킷 방향** | 클라이언트와 서버가 서로를 확인 | 양방향 통신을 각각 독립적으로 종료 |
| **핵심 플래그** | **SYN, ACK** | **FIN, ACK** |
| **데이터 전송** | 이후에 본격적으로 데이터 전송 시작 | 남은 데이터를 모두 보내고 안전하게 종료 |

3way는 요청 -> 확인 후 전송 -> 확인의 3단계
4way는 요청 -> 남아있는 데이터 확인 -> 전송 -> 확인의 4단계

-> 남아있는 데이터를 처리하는 과정이 추가되어 1단계가 늘어남

---
## 브로드캐스트 , 멀티캐스트 , 유니캐스트 , 애니캐스트
- **브로드캐스트**
    - 1개의 송신자가 **같은 LAN 내의 모든 수신자**에게 데이터를 동시에 전송하는 방식
    - **범위**: 같은 LAN 내
    - **활용**: ARP 요청(맥 주소 검색) , DHCP(IP 주소 할당) 등
- **멀티캐스트**
    - **단일로 전송한 데이터를** 수신하고자 하는 **특정 그룹의 수신자들**에게만 동시에 패킷을 보내는 방식
    - **범위**: 일반적으로 LAN 내에서 사용하지만 외부망으로 나갈 수는 있음
    - **활용**: IPTV, 스트리밍, 실시간 금융 정보 등
- **유니캐스트**
    - MAC 주소를 통해 다른 시스템에 부하를 주지 않으며 **1대1로 데이터를 전송하는 통신 방식**
    - **범위**: 인터넷 등 외부망 가능
    - **활용**: 일대일 통신, 웹사이트 접속, 파일 전송 등
- **애니캐스트**
    - **같은 IP주소**를 사용하는 서버 중 **가장 가까운, 빠른 서버**를 **찾아 연결시켜주는** 통신 방식
    - **범위**: 일반적으로 광역망에서 활용
    - **활용**: 디도스 방어, DNS 서버, 글로벌 웹 서비스 등
---
## TCP/UDP

#### TCP
**신뢰성을 우선으로 하는 연결 지향적 프로토콜**
- 특징
    - 3way handshake와 4way handshake를 통한 연결 신뢰성
    - 보내는 순서대로 도착하도록 보장
    - 유니캐스트 기반으로 동작  
- 장점
    - 데이터 신뢰성이 매우 높다
- 단점
    - 연결 확인 과정때문에 UDP보다 속도가 느리다
- 활용
    - 웹 서핑, 이메일 전송, 파일 전송 등 데이터 신뢰성이 필요한 서비스

#### UDP
**속도와 효율성을 우선하는 비연결형 프로토콜**
- 특징
    - 연결(handshake)의 과정이 없다
    - 확인의 절차가 없고 순서도 보장하지 않는다
    - 확인 절차가 없어 오버헤드(추가 작업)가 적다
- 장점
    - 매우 빠르고 실시간성이 뛰어나다
- 단점
    - 중간에 유실된 패킷은 알 수 없고 복구도 불가능하다
- 용도
    - 온라인 게임, 실시간 스트리밍 등 실시간성이 중요한 서비스
---
## OSI 7계층
> 네트워크 통신 과정을 7개의 단계로 나눈 표준 모델로 복잡한 통신 과정을 단계별로 나누어 문제 발생 시 어느 단계에서 오류가 났는지 파악하기 쉽게 도와줌/ 각 계층은 독립적으로 작동할 수 있음
#### 1계층
- 물리계층
- 데이터를 전기신호로 바꾸어 전송
- 전송 단위: bit
- 대표 장비: 케이블, 허브, 리피터

#### 2계층
- 데이터 링크 계층
- 물리 계층을 통한 데이터 에러 검출 및 흐름 제어
- 전송 단위: 프레임(Frame)
- 대표 장비: MAC 주소(주소체계), 스위치
#### 3계층
- 네트워크 계층
- 데이터를 목적지까지 최적의 경로로 유통
- 전송 단위: 패킷(packet)
- 대표 장비 및 프로토콜: IP, 라우터
#### 4계층
- 전송 계층
- 종단 간 신뢰성 있는 전송
- 전송 단위: 세그먼트
- 프로토콜: TCP/UDP
#### 5계층
- 세션계층
- 통신장치간의 논리저 연결 관리
- 전송 단위: 데이터
#### 6계층
- 표현 계층
- 데이터 형식 변환, 암호화, 압축
- 전송 단위: 데이터
- 대표 장비 및 프로토콜: JPEG, MPEG, SSL/TLS
#### 7계층
- 응용 계층
- 사용자가 네트워크에 접속하는 인터페이스
- 전송 단위: 데이터
- 프로토콜: HTTP/SMTP/FTP
---
## 브리지
> 데이터링크 계층에서 동작하는 네트워크 연결 장비
#### 브리지의 4가지 핵심 동작
1. Learning (학습)
    - 수신 프레임의 출발지 MAC 주소와 포트를 MAC 테이블에 저장

2. Flooding (범람)
    - 목적지 MAC 주소가 MAC 테이블에 없을 경우
    - 모든 포트로 프레임 전송 (브로드캐스트, Unknown Unicast)

3. Forwarding (전달)
    - 목적지 MAC 주소가 MAC 테이블에 존재하면
    - 해당 포트로만 프레임 전달

4. Filtering (차단)
    - 목적지 MAC이 동일 포트에 존재하면
    - 프레임을 전달하지 않음 (트래픽 감소)

## 브리지를 왜 사용하지 않을까
1. 처리 메커니즘
    - 브리지: 프레임 처리를 CPU의 소프트웨어 명령어로 수행함 -> 트래픽이 증가하면 CPU 부하가 급증해 병목현상이 발생하고 높은 지연을 유발함
    - 스위치: 브리지를 하드웨어화 하여 지연 시간을 낮춤
2. 포트 밀도 및 확장성
    - 브리지: 보통 2~4개의 소수 포트만 지원하고 네트워크를 큰 덩어리로 나누는 역할에 그침
    - 스위치: 고밀도 포트(24, 48포트 이상)를 지원하여 모든 단말기를 스위치에 직접 연결할 수 있게 해 효율적인 성형(Star) 구조를 만듬
---
## 스위치
> 스위치는 데이터 링크 계층에서 작동하며, 유입되는 프레임의 MAC 주소를 참조하여 목적지 포트로만 데이터를 전달하는 장비입니다.

1. **ASIC(Application Specific Integrated Circuit) 기반 처리**
    -  소프트웨어가 아닌 전용 하드웨어 칩(ASIC)을 사용해 프레임을 처리하여 Wire Speed(케이블이 허용하는 최대 속도)를 구현

2. **Micro-segmentation (마이크로 세그멘테이션)**
    - 각 포트마다 독립적인 콜리전 도메인(Collision Domain)을 할당해 포트 간 데이터 충돌을 원천적으로 차단

3. **Full-Duplex (전이중 통신)**
    - 송신과 수신이 동시에 가능하여 반이중 통신 방식보다 대역폭 효율이 2배 높음

