# linux
---
## /etc 디렉토리란?
> 리눅스/유닉스 시스템에서 시스템 설정과 구성 파일을 저장하는 디렉토리
---
#### /etc 디렉토리의 핵심 파일들
---
##### 사용자 인증 관련
- **/etc/passwd**
    - 사용자의 계정 정보를 포함하는 문서(과거에는 사용자의 패스워드를 저장)
    - 계정이름:비밀번호(x):UID:GID:사용자정보:홈디렉토리:로그인쉘 의 형태로 저장됌
- **/etc/shadow**
    - 암호화된 비밀번호와 비밀번호 관리 정책을 저장하는 문서
    - root계정만 접근 가능함
- **/etc/group**
    - 그룹 관련 정보를 저장하는 문서
    - 어떤 사용자가 어떤 그룹에 속해있는지 정의
- **/etc/gshadow**
    - 그룹 비밀번호 및 관리자의 정보를 저장하는 문서
    - shadow의 그룹 버전
---
##### 네트워크 관련
- **/etc/hosts**
    - 도메인 -> IP 직접 매핑한 내용을 저장하는 문서
- **/etc/hosts.conf**
    - 도메인에 대한 IP주소값을 찾을 때 어디서 찾을 지 결정하는 문서
    - **과거에 사용했던 문서**
    
- **/etc/nsswitch.conf**
    - 현재 hosts.conf의 역할을 하는 문서
- **/etc/resolv.conf**
    - 사용하고자 하는 네임 서버를 지정하는 문서
    ---
## DNS의 동작 흐름
---
###  Local DNS가 도메인을 아는 경우
 1. 사용자가 웹 브라우저에 **도메인을 입력** -> **Local DNS**에 도메인에 대한 **IP주소 요청**
 2. Local DNS에서 주소를 찾아 반환 

Local DNS에는 **통신사 DNS**, **공유기 DNS**, **공용 DNS**(google DNS , cloudflare DNS) 등등이 있음 
### Local DNS가 도메인을 모르는 경우(Recursive Query/재귀적 질의)
1. Local DNS에서 Root DNS에 **쿼리를 보냄**(쿼리 = 요청)
2. Root DNS에서 TLD 부분을 보고 **Local DNS에게 해당 TLD DNS의 서버 정보를 알려줌**
3. Local DNS가 **TLD DNS에 쿼리 보냄**
4. TLD DNS는 TLD를 기준으로 **Authoritative DNS 서버의 정보를 알려줌**
5. Authoritative DNS 서버에서 **해당 도메인의 IP주소를 반환함**
### Local DNS에서 도메인을 어떻게 기억할까
Local DNS에선 위의 과정을 통해 얻은 도메인과 IP주소를 **캐시로 저장**한다 이는 당연한 이야기지만 DNS서버마다 따로 저장되어있다

그렇기 때문에 일정 시간이 지나거나 해 Local DNS 서버의 캐시가 삭제된 게 아니라면 DNS 쿼리 과정을 거치지 않아도 된다

---
## DNS vs hosts
**DNS** = 서버에 저장되어있는 도메인을 기반으로 IP 주소를 찾아주는 **네트워크 서비스**
**hosts** = **내 컴퓨터**에 저장되어있는 도메인 매핑 파일

---

### 일반적인 작동 흐름
1. 도메인 입력 후 hosts에서 해당 도메인을 찾을 수 있을 떄는 바로 그 IP주소로 진입
2. hosts에 없는 도메인일 경우 DNS 서버에 해당 도메인의 IP주소를 요청
---
### hosts의 용도
본인의 컴퓨터에서만 특정하게 주소를 찾을 수 있기 때문에 **특정 사이트 차단, 개발 테스트 등**에 사용됌 

---
## UID와 GID
> 컴퓨터는 기본적으로 여러 사람이 함께 사용할 수 있도록 만들었다. 여러 사람이 함께 사용하기 위해서는 각 사람마다 식별이 필요한데, 리눅스 운영체제에서는 UID , GID를 통해 사용자를 구분한다.
- UID
    - 사용자를 구분하기 위한 번호
    - 일반적으로 0은 root , 1 ~ 999 는 시스템 계정 , 1000 이상은 일반 사용자 계정으로 사용합

- GID
    - 사용자가 속한 그룹을 구분하는 번호
    - 많은 사용자들의 관리를 용이하게 하기 위해 그룹을 사용
---
## root계정
> 파일 읽기/쓰기/삭제 , 사용자 생성/삭제 , 시스템 설정 변경 등 모든 권한을 가지고있는 슈퍼유저

#### root계정의 특징
- UID가 0으로, 시스템은 UID가 0인 계정을 관리자로 인식
- 모든 시스템 제어 권한을 가짐

**일반 사용자도 sudo등의 명령어를 통해 일시적으로 관리자 권한을 획득할 수 있음**
